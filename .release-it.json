{
  "buildCommand": "sed -i -r 's/^Stable tag: .*/Stable tag: ${version}/g' readme.txt; npm run release -- ${version}",
  "github": {
    "release": true,
    "assets": "vanilla-${version}.zip"
  },
  "npm": {
    "publish": false
  }
}
